<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description, autofocus: true, placeholder: '3cl' %>
  <%= f.input :ingredient_id, collection: @ingredients, as: :select %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
